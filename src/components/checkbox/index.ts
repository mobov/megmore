import MCheckbox from '@/components/checkbox/checkbox'

/* istanbul ignore next */
MCheckbox.install = function install(Vue: any) {
    Vue.component(MCheckbox.name, MCheckbox)
}
export default MCheckbox

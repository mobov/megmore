/**
 * Created by nocoolyoyo on 2018/6/28.
 */

export default {
    name: 'm-loading',
    inserted (el, binding) {
        // const callback = binding.value
        // const options = binding.options || { passive: true }
    },
    unbind (el) {
        // const { callback, options } = el._onResize
        //
        // window.removeEventListener('resize', callback, options)
        // delete el._onResize
    }
}